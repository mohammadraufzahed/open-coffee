import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "ir.mizteam.miz",
  appName: "Miz",
  webDir: "build",
  bundledWebRuntime: false,
};

export default config;
